namespace MyMath {
    export namespace Circle {
        const PI = 3.14;

        export function calculateRectangle(width: number, length: number) {
            return width * length;
        }
    }
}